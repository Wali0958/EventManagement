<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    @*<link href="~/Content/bootstrap.css" rel="stylesheet" />*@
    <link href="~/Content/jquery-ui.css" rel="stylesheet" />
    <link href="~/Content/dataTables.dataTables.min.css" rel="stylesheet" />
    <link href="~/Content/custom.css" rel="stylesheet" />
    <link href="~/Content/Site.css" rel="stylesheet" />
    <link href="~/Content/all.min.css" rel="stylesheet" />
    <style>
        sticky-top {
            position: sticky;
            top: 0;
            z-index: 1020; /* keeps it above body content */
        }

        .table thead.sticky-top th {
            background-color: #fff; /* keep header background solid */
            box-shadow: 0 2px 2px rgba(0,0,0,.1);
        }
    </style>
</head>
<body class="bg">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow-sm">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center" href="#">
                <img src="~/Content/images/logo.jpeg" height="40" class="me-2" />
                <span class="fw-bold">Exhibitions Services</span>
            </a>
            <button class="navbar-toggler" type="button"
                    data-bs-toggle="collapse"
                    data-bs-target="#mainNavbar"
                    aria-controls="mainNavbar"
                    aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="mainNavbar">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0 small btn-sm btn-outline-info">

                    @if (Session["UserEmail"] != null)
                    {
                        try
                        {
                            if (Session["RoleType"].ToString().Trim() == "S")
                            {
                                <li class="nav-item">@Html.ActionLink("Kiosk Search", "PrintwithKiosk", "Home", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Search / Print Badge", "MasterSearchBadge", "Home", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Search / Print Badge New", "MasterSearchBadgeNew", "Home", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Bulk Print Badge", "Index", "Home", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Bulk Print Badge New", "IndexNew", "Home", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Add New Badge", "AddNewBadge", "Home", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Add New Badge Camera", "AddNewBadgeCamera", "Home", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Scanning", "Scanning", "Home", null, new { @class = "nav-link" })</li>
                            }
                            else if (Session["RoleType"].ToString().Trim() == "GS")
                            {
                                <li class="nav-item">@Html.ActionLink("Scanning", "Scanning", "Home", null, new { @class = "nav-link" })</li>
                            }
                            else if (Session["RoleType"].ToString().Trim() == "A" || Session["RoleType"].ToString().Trim() == "M")
                            {
                                <li class="nav-item">@Html.ActionLink("Dashboard", "AdminDashboard", "Admin", null, new { @class = "nav-link" })</li>

                                if (Session["RoleType"].ToString().Trim() == "A")
                                {
                                    <li class="nav-item">@Html.ActionLink("Add Event", "MasterEvent", "Admin", null, new { @class = "nav-link" })</li>
                                }

                                <li class="nav-item">@Html.ActionLink("Category", "MasterEventCategory", "Admin", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Users", "AddMasterUser", "Admin", null, new { @class = "nav-link" })</li>

                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                        Master Columns Add
                                    </a>
                                    <ul class="dropdown-menu">
                                        <li>@Html.ActionLink("Add Columns for Event", "MasterShowColEvent", "Admin", null, new { @class = "dropdown-item" })</li>
                                        <li>@Html.ActionLink("Add Columns for Certificate", "MasterShowColEventCertificate", "Admin", null, new { @class = "dropdown-item" })</li>
                                    </ul>
                                </li>
                                <li class="nav-item dropdown">
                                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                        Master Print Setup
                                    </a>
                                    <ul class="dropdown-menu">
                                        <li>@Html.ActionLink("Print Setup Badge", "MasterPrintSetupEvent", "Admin", null, new { @class = "dropdown-item" })</li>
                                        @*<li>@Html.ActionLink("Print Setup Certificate", "MasterPrintSetupEventCertificate", "Admin", null, new { @class = "dropdown-item" })</li>*@
                                        <li>@Html.ActionLink("Print Setup (Drag Drop)", "PrintSetupTemplate", "Admin", null, new { @class = "dropdown-item" })</li>
                                        <li>@Html.ActionLink("Print Certificate Setup (Drag Drop)", "PrintSetupCertificateTemplate", "Admin", null, new { @class = "dropdown-item" })</li>
                                    </ul>
                                </li>
                                if (Session["RoleType"].ToString().Trim() == "A" || Session["RoleType"].ToString().Trim() == "S")
                                {
                                    <li class="nav-item dropdown">
                                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                                            Master Excel
                                        </a>
                                        <ul class="dropdown-menu">
                                            <li class="nav-item">@Html.ActionLink("Upload Excel", "MasterExcel", "Admin", new { area = "" }, new { @class = "dropdown-item" })</li>
                                            <li class="nav-item">@Html.ActionLink("Master Email (Event Wise)", "MasterSendmail", "Admin", new { area = "" }, new { @class = "dropdown-item" })</li>

                                        </ul>
                                    </li>
                                }
                                <li class="nav-item">@Html.ActionLink("Master Badge", "MasterBadge", "Admin", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Bulk QR", "BulkQR", "Admin", null, new { @class = "nav-link" })</li>
                                <li class="nav-item">@Html.ActionLink("Kiosk Setting", "KioskSetting", "Admin", null, new { @class = "nav-link" })</li>
                            }
                            if (Session["RoleType"].ToString().Trim() == "A" ||
                                Session["RoleType"].ToString().Trim() == "M" ||
                                Session["RoleType"].ToString().Trim() == "S")
                            {
                                <li class="nav-item">@Html.ActionLink("Reports", "Report", "Home", null, new { @class = "nav-link" })</li>
                            }
                        }
                        catch
                        {
                            Response.RedirectToRoute(new { controller = "Home", action = "Login" });
                        }
                    }
                </ul>

                <!-- Right side -->
                @if (Session["UserEmail"] != null)
                {
                    <div class="d-flex align-items-center">
                        <span class="text-white small me-3">
                            <i class="fa fa-user"></i> @Session["UserEmail"]
                        </span>
                        @Html.ActionLink("Logout", "LogOut", "Home", null, new { @class = "btn btn-sm btn-outline-danger" })
                    </div>
                }
            </div>
        </div>
    </nav>
   
        @if (Session["RoleType"].ToString().Trim() == "S" || Session["RoleType"].ToString().Trim() == "GS")
        {
            <div class="row g-2 mb-3 text-center">
                <div class="col-12 col-md-4">
                    <div class="card border-0 shadow-sm bg-primary text-white">
                        <div class="card-body p-2">
                            <i class="fa fa-id-badge fa-2x"></i>
                            <h6 class="mt-1 mb-0">Total Badge</h6>
                            <h5>@ViewBag.TotalBadge</h5>
                        </div>
                    </div>
                </div>
                <div class="col-12 col-md-4">
                    <div class="card border-0 shadow-sm bg-warning text-dark">
                        <div class="card-body p-2">
                            <i class="fa fa-gift fa-2x"></i>
                            <h6 class="mt-1 mb-0">Kit Collected</h6>
                            <h5>@ViewBag.TotalKit</h5>
                        </div>
                    </div>
                </div>
                <div class="col-12 col-md-4">
                    <div class="card border-0 shadow-sm bg-success text-white">
                        <div class="card-body p-2">
                            <i class="fa fa-print fa-2x"></i>
                            <h6 class="mt-1 mb-0">Badge Printed</h6>
                            <h5>@ViewBag.TotalBadgePrint</h5>
                        </div>
                    </div>
                </div>
            </div>
        }
        @RenderBody()
        <footer class="mt-4 p-3 text-center bg-light rounded shadow-sm">
            <small><span class="text-danger fw-bold"><i class="fa fa-phone"></i> Nadeem – 8368352220</span><br />© @DateTime.Now.Year – IT Harb Solution</small>
        </footer>
   
    <script src="~/Scripts/jquery-3.7.1.min.js"></script>
    <script src="~/Scripts/jquery.validae.js"></script>
    <script src="~/Scripts/jquery-ui.min.js"></script>
    <script src="~/Scripts/bootstrap.bundle.min.js"></script>
    @*<script src="~/Scripts/bootstrap.js"></script>*@
    <script src="~/Scripts/all.min.js"></script>
    <script src="~/Scripts/dataTables.min.js"></script>
    @*<script src="https://cdn.ckeditor.com/ckeditor5/41.3.0/classic/ckeditor.js"></script>*@
    <script src="~/Scripts/sweetalert2@11.js"></script>
    <div class="modal" tabindex="-1" id="mdlmsg" data-bs-backdrop="static" data-bs-keyboard="false">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content" style="background-color:#F1F1F1">
                <div class="modal-header">
                    <h5 class="modal-title"><b>Alert</b></h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    @if (@TempData["Msg"] != null)
                    {
                        <p><strong>@TempData["Msg"]</strong></p>
                    }
                </div>
            </div>
        </div>
    </div>
    <script>
     $(document).ready(function () {
         if ('@TempData["Msg"]' !== "") {            
             $("#mdlmsg").modal('show');
         }
     });
    </script>
    <script>
        new DataTable('.mtable');
    </script>
    <script>
        $(document).ready(function () {
            $('.Mobile').on('keypress', function (e) {
                if ($.inArray(e.keyCode, [8, 9, 27, 13]) !== -1 ||
                    (e.keyCode == 65 && (e.ctrlKey === true || e.metaKey === true)) ||
                    (e.keyCode == 67 && (e.ctrlKey === true || e.metaKey === true)) ||
                    (e.keyCode == 86 && (e.ctrlKey === true || e.metaKey === true)) ||
                    (e.keyCode == 88 && (e.ctrlKey === true || e.metaKey === true)) ||
                    (e.keyCode >= 35 && e.keyCode <= 39)) {
                    return;
                }
                if ((e.shiftKey || (e.keyCode < 48 || e.keyCode > 57))) {
                    e.preventDefault();
                }
            });
        });
    </script>
    <script>
        $('.alphanumericOnly').on('input', function () {
            $(this).val(function (_, val) {
                return val.replace(/[^a-zA-Z0-9 ]/g, '');
            });
        });
    </script>
    <script>
        $('.varcharOnly').on('input', function () {
            $(this).val(function (_, val) {
                return val.replace(/[^a-zA-Z\s ]/g, '');
            });
        });
    </script>
    <script>
        $('.decmal').on('input', function () {
            let value = $(this).val();
            let newValue = value.replace(/[^0-9. ]/g, '').replace(/(\..*)\./g, '$1');
            $(this).val(newValue);
        });
    </script>
    <script type="text/javascript">
        $('#Event').on('change', function () {
            var RegistrantID = $(this).val();
            $.post('@Url.Action("GetCategoryByEvent", "Admin")', {
                "EventId": RegistrantID
            }, function (data, status) {
                $("#CategoryId").empty();
                $("#CategoryId").append(data);
            });
        });
    </script>
    <script>
        $(document).ready(function () {
            $('input[type=radio][name=IsPrint]').change(mGetBadgeFilter);
            // Call on Category dropdown change
            $('#Category').change(mGetBadgeFilter);
            // Call on Company dropdown change
            $('#company').change(mGetBadgeFilter);
        });
    </script>
    <script>
        function mGetBadgeFilter() {
            var PrintStatus = $('input[type=radio][name=IsPrint]:checked').val();
            var cate = $("#Category").val();
            var comp = $("#company").val();
            $.ajax({
                url: '/Home/GetBadgeByFilter',
                type: 'GET',
                data: { IsPrint: PrintStatus, category: cate, Company: comp },
                success: function (html) {
                    $('#tblprint tbody').html(html);
                    $("#multipleprintbtn").removeClass("d-none");
                },
                error: function (xhr, status, error) {
                    console.error("Error: " + error);
                }
            });
        }
    </script>
    <script>
        $(document).ready(function () {
            $('#BadgeId').on('focus', function () {
                $(this).select();
            });
            $('#BadgeId').focus();
        });
    </script>
    <script>
        $(document).ready(function () {
            let typingTimer=null;
            const typingDelay = 400; // wait time (ms) after user stops typing

            function searchData() {
                const search = $("#txtSearch").val().trim();

                if (search === "") {
                    $('#tbodysearch').html("<tr><td colspan='8' class='text-danger text-center'>Please enter or scan something to search.</td></tr>");
                    return;
                }

                $.ajax({
                    url: '/Home/SearchResult',
                    type: 'GET',
                    data: { text: search },
                    beforeSend: function () {
                        $('#tbodysearch').html("<tr><td colspan='8' class='text-center text-muted'>Searching...</td></tr>");
                    },
                    success: function (response) {
                        if (response.success) {
                            $('#tbodysearch').html(response.mData);
                        } else {
                            $('#tbodysearch').html("<tr><td colspan='8' class='text-danger text-center'>" + response.message + "</td></tr>");
                        }
                    },
                    error: function () {
                        $('#tbodysearch').html("<tr><td colspan='8' class='text-danger text-center'>An unexpected error occurred.</td></tr>");
                    }
                });
            }

            // 🔹 Trigger search on Enter key (works for QR scan too)
            $('#txtSearch').on('keypress', function (e) {
                if (e.which === 13) {
                    e.preventDefault();
                    searchData();
                }
            });

            // 🔹 Trigger search as user types (with small delay)
            $('#txtSearch').on('keyup', function (e) {
                clearTimeout(typingTimer);
                if (e.which !== 13) { // skip Enter, already handled above
                    typingTimer = setTimeout(searchData, typingDelay);
                }
            });

            // 🔹 Manual search button
            $('#btnsearch').on('click', searchData);
        });
    </script>
    <script>
        $(document).ready(function () {
            $('#EventId').on('change', function () {
                var selectedValue = $(this).val();
                $.ajax({
                    url: '/Admin/GetColumnsByFilter',
                    type: 'GET',
                    data: { EventId: selectedValue },
                    success: function (html) {
                        $("#mdiv").removeClass("d-none");
                        $("#abutt").removeClass("d-none");
                        $('#tblprint tbody').html(html.partial1);
                        $('#mprintshowdata').html(html.partial2);
                        $("#multipleprintbtn").removeClass("d-none");
                        //$('#mprintshowdata').html(html);
                    },
                    error: function (xhr) {
                        //console.log("check data", html);
                        console.log("error code", xhr);
                        $("#mdiv").addClass("d-none");
                        $("#abutt").addClass("d-none");
                        alert('An error occurred.');
                    }
                });
            });
        });
    </script>


    <script>
        $(document).ready(function () {
            $('#EventIdCertificate').on('change', function () {
                var selectedValue = $(this).val();
                $.ajax({
                    url: '/Admin/GetColumnsByFilterCertificate',
                    type: 'GET',
                    data: { EventId: selectedValue },
                    success: function (html) {
                        $("#mdiv").removeClass("d-none");
                        $("#abutt").removeClass("d-none");
                        $('#tblprint tbody').html(html.partial1);
                        $('#mprintshowdata').html(html.partial2);
                        $("#multipleprintbtn").removeClass("d-none");
                        //$('#mprintshowdata').html(html);
                    },
                    error: function (xhr) {
                        //console.log("check data", html);
                        console.log("error code", xhr);
                        $("#mdiv").addClass("d-none");
                        $("#abutt").addClass("d-none");
                        alert('An error occurred.');
                    }
                });
            });
        });
    </script>






    @*<script>
            $(document).ready(function () {

                function searchData() {
                    var search = $("#txtSearch").val().trim();

                    $.ajax({
                        url: '/Home/SearchResult',
                        type: 'GET',
                        data: { text: search },
                        success: function (response) {
                            if (response.success) {
                                $('#tbodysearch').html(response.mData);
                            } else {
                                $('#tbodysearch').html("<tr><td colspan='8' class='text-danger text-center'>" + response.message + "</td></tr>");
                            }
                        },
                        error: function (xhr) {
                            console.log("error code", xhr);
                            alert('An unexpected error occurred.');
                        }
                    });
                }

                // Button click
                $('#btnsearch').on('click', searchData);

                // Enter key in textbox
                $('#txtSearch').on('keypress', function (e) {
                    if (e.which === 13) {
                        e.preventDefault();
                        searchData();
                    }
                });
            });
        </script>*@
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            const textbox = document.getElementById('BadgeId');
            if (textbox) {
                textbox.addEventListener('focus', function () {
                    this.select(); // selects all text when focused
                });
            }
        });
    </script>
    <script>
    document.addEventListener("DOMContentLoaded", function () {
        const el = document.querySelector('#ckeditor');
        if (el) {
            ClassicEditor
                .create(el, {
                    toolbar: [
                        'bold', 'italic', 'underline',
                        '|',
                        'numberedList', 'bulletedList',
                        '|',
                        'link',
                        '|',
                        'undo', 'redo'
                    ]
                })
                .catch(error => {
                    console.error("CKEditor Error:", error);
                });
        }

    });
    </script>
    <script>
        $(document).ready(function () {
            $(".datepicker").datepicker({
                dateFormat: "dd-mm-yy",
                changeMonth: true,
                changeYear: true
            });
        });
    </script>
</body>
</html>
